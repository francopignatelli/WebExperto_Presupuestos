<section class="section-ver-presupuesto">
  <%= render partial: 'shared/header', locals: { view: false } %>
  <h1>Add product</h1>

  <%= render "form", lineitem: @lineitem %>

  <br>

  <div class="volver-presupuesto">
    <%= link_to "Volver al presupuesto", edit_budget_path(@lineitem.budget), class: "back-button"  %>
  </div>
</section>
